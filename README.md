# FastAPI + Vue 3 项目管理系统（增强版）

## ✨ 功能
- 用户注册 / 登录（带图形验证码）
- JWT 认证 + 角色权限（admin/user）
- 项目管理（CRUD）
- 用户管理（仅 admin 可见）
- 忘记密码（演示模式）

## 🚀 快速启动

### 后端
```bash
cd backend
pip install -r requirements.txt
uvicorn main:app --reload --port 8000 